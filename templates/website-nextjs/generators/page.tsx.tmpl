export const dynamic = "force-dynamic";

export default function {name_pascal}Page() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold">{name_pascal}</h1>
      <p className="mt-2 text-sm text-neutral-500">
        New page at /{name_kebab}.
      </p>
    </main>
  );
}
