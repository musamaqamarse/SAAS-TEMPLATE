export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>__PROJECT_NAME__ — API</h1>
      <p>This is a headless backend. Hit <code>/api/health</code> to check.</p>
    </main>
  );
}
