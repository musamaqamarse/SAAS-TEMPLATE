/** Domain model for {name_kebab}. Edit fields to match your schema. */
export interface {name_pascal} {
  id: string;
  createdAt: string;
  updatedAt?: string;
}

/** Payload for creating a new {name_kebab}. */
export interface {name_pascal}Create {}

/** Patch payload for {name_kebab} — every field optional. */
export interface {name_pascal}Update {}
