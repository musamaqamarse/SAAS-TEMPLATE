# @create-saas/marketing

Public landing page for `create-saas`. Skeleton — Phase 7 fills in showcase, comparisons, and richer copy.

```bash
pnpm --filter @create-saas/marketing dev   # http://localhost:3020
```

Set `NEXT_PUBLIC_APP_URL` to point CTAs at the deployed configurator (defaults to `http://localhost:3010`).
